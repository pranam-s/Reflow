"""``reflow demo``: the single scripted command the pitch video records.

See :mod:`reflow.demo.runner` for the orchestration entry point and
:mod:`reflow.demo.data` for exactly which committed artefacts every number
shown comes from.
"""
